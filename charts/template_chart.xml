<?xml version="1.0" encoding="utf-8"?>
<container id='' name='' status=''>

    <infrastructure>
        <hostname name='' />
        <container_engine>
            <docker version=''>yes/no</docker_v>
            <containerd version=''>yes/no</containerd>
            <runc version=''>yes/no</runc>
        </container_engine>
        <os name='' type='' architecture='' />
        <kernel version='' />
        <cpus number='' />
        <mem total='' />
        <registry name='' />
    </infrastructure>

    <image base_image=''> 
        <id object='image'></id>
        <repo name='' />
        <tag name='' />
        <t_created object='image'></t_created>
        <size object='image' value='' />
    </image>

###
The following config represent the (run-time) configuration of the container. If no paramenters are specified
at running time (e.g. docker run --env), the config will be the same as the Dockerfile. In this regard, we 
have two options:
 - keep both configurations (Dockerfile and runtime) to compare them;
 - overwrite the Dockerfile config with the runtime paramenters and keep only one version.
###

    <config object='container'>
        <user><user/>
        <env></env>
        <volume></volume>
        <network adapter=''></network>
        <entrypoint command=''>
        <cmd command=''>
    </config>

    <permissions>
        <files values='' />
        <network values='' />
        <processes values='' />
        <adminop values='' />
    </permissions>

    <timeline object='container'>
        <start_t object='container' value='' />
        <stop_t object='container' value='' />
    </timeline>

</container>